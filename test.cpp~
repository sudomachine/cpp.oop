#include <iostream>

int main()
{
  int size = 10;
  int* arr = new int[size];
  arr[size--];

  for (int i = 0; i < size; i++)
    {
      std::cout << i << " " << arr[i] << std::endl;
    }
  return 0;
}
